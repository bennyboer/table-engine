/**
 * Types of events that may occur in the table-engine.
 */
export enum TableEngineEventType {

	/**
	 * Emitted, when the table renderer is ready for rendering
	 * after initializing the table engine.
	 */
	RENDERER_READY

}
